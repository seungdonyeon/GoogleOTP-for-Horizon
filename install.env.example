# OTPWeb installer configuration
# This file is sourced by install.sh (must be in the same directory).
# Tip: Keep secrets empty to be prompted during install.

# -------------------------
# Project directory
# -------------------------
# If empty, install.sh will use its own directory.
INSTALL_DIR="/otpweb"

# -------------------------
# Network / Bind
# -------------------------
# 0.0.0.0 = listen on all interfaces (default)
# 127.0.0.1 = localhost only
ADMIN_BIND="0.0.0.0"
QR_BIND="0.0.0.0"

# -------------------------
# Service ports
# -------------------------
ADMIN_PORT=8000
QR_PORT=5000

# -------------------------
# HTTPS (self-signed)
# -------------------------
# If enabled, install.sh will generate a self-signed certificate (5 years)
# and run both services over HTTPS.
ENABLE_HTTPS=1

# For internal service-to-service calls using the self-signed certificate.
# 1 = allow self-signed cert (recommended with ENABLE_HTTPS=1)
ALLOW_SELF_SIGNED_TLS=1

# -------------------------
# OTP label/issuer (shown in authenticator apps)
# -------------------------
# Google Authenticator typically displays "Issuer (Account)".
# -------------------------
# Google Authenticator display name
# -------------------------
# This single value controls BOTH the issuer and the label prefix shown in authenticator apps.
# Example display: "<OTP_DISPLAY_NAME>" and "<OTP_DISPLAY_NAME>:<username>"
OTP_DISPLAY_NAME="VDI"

# -------------------------
# AD Join - Winbind (optional)
# -------------------------
AD_JOIN=1
AD_REALM="EXAMPLE.LOCAL"      # UPPERCASE realm
AD_WORKGROUP="EXAMPLE"        # NetBIOS name / workgroup
AD_JOIN_USER="Administrator"  # account with join rights
AD_JOIN_PASS=""               # if empty, install.sh will prompt (recommended)

# -------------------------
# FreeRADIUS (Horizon / RADIUS client)
# -------------------------
# Horizon Connection Server IP address (RADIUS client).
# If empty, install.sh will prompt.
# Example: 192.168.10.151
RADIUS_CLIENT_IP=""

# Shared secret between Horizon and FreeRADIUS.
# If empty, install.sh will prompt (blank => defaults to VMware1!).
RADIUS_SECRET=""

# -------------------------
# Installer mode
# -------------------------
# auto   : use offline bundle if ./packages exists, otherwise online
# offline: force offline mode (requires ./packages)
# online : force online mode (dnf from configured repos)
OFFLINE_MODE="auto"
